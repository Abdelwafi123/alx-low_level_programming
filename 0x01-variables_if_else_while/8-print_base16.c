#include <stdio>

/**
 * main - Entry point
 *
 * Description: print numbers of base16 in lowercase
 *
 * Return: 0 (Success)
*/

int main(void)
{
	int ch = 48;

	while (ch <= 102)
	{
		putchar(ch);
		if (ch == 57)
			ch += 57
		ch++;
	}
	putchar('\n');

	return (0);
}
